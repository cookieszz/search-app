import { ButtonProps } from "@material-ui/core";
import React from "react";
import { CustomSubmitBtn } from "./CustomSubmitBtn.styles";

export default (props: ButtonProps) => <CustomSubmitBtn {...props} />;
