import { makeStyles } from "@material-ui/core";

export const useAppRootStyles = makeStyles(() => ({
  appRoot: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
  },
}));
