import { OutlinedInputProps } from "@material-ui/core";
import React from "react";
import { CustomOutlinedInput } from "./CustomOutlinedInput.styles";

export default (props: OutlinedInputProps) => (
  <CustomOutlinedInput {...props} />
);
